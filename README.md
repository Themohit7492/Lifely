Lifely — Blood, Plasma, Platelet & Organ Donation Platform

A full-stack React + Express + MongoDB starter implementing the requested Lifely donor/recipient platform.

Set `MONGO_URI`, `JWT_SECRET`, and `CLIENT_URL` in `server/.env`.

The client expects the API at `https://lifely-s5pe.onrender.com/api`.

